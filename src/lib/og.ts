import asset from "@/assets/scomas-og.jpg.asset.json";

/** Absolute URL for the default social preview image. Crawlers require absolute. */
export const OG_IMAGE = `https://scomaspro.lovable.app${asset.url}`;
